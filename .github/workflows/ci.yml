name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build test image (green = every check below passed)
        run: docker build -f Dockerfile.ci -t tracelean-ci .
        working-directory: tracelean
